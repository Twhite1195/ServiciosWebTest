@model IEnumerable<ServicioWebTest2.Models.Bitacora>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Cod_Usuario)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fecha_Hora)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tipo_Bitacora.Tipo_Bitacora1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Cod_Usuario)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Fecha_Hora)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tipo_Bitacora.Tipo_Bitacora1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Cod_Bitacora }) |
            @Html.ActionLink("Details", "Details", new { id=item.Cod_Bitacora }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Cod_Bitacora })
        </td>
    </tr>
}

</table>
