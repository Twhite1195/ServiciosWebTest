@model IEnumerable<ServicioWebTest2.Models.Disco>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombre_Disco)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre_Disco)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_Disco }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_Disco }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_Disco })
        </td>
    </tr>
}

</table>
